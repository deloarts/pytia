#!/usr/bin/env python

# Let's hope Github allows to detect the package.
# Setup.py is obsolete, build is done with poetry.

import setuptools

if __name__ == "__main__":
    setuptools.setup(name="pytia")
